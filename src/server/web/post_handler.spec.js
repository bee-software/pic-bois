/*global describe, spyOn, it, expect */
describe("Post handler", function () {

    var PostHandler = require("./post_handler");

    it("keeps data chunk on data event", function (done) {
//        var action = function(scoredBy, assistedBy) {};
//        spyOn(action);
//
//        var postHandler = new PostHandler(action);
//
//        postHandler.handle()
        done();
    });

});